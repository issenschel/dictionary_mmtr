package repository;

import java.nio.file.Path;

public class LatinDictionaryRepository extends BaseFileDictionary {
    public LatinDictionaryRepository(Path dectionaryPath) {
        super(dectionaryPath);
    }
}
