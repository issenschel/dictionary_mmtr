package repository;

import java.nio.file.Path;

public class NumberDictionaryRepositoryRepository extends BaseFileDictionaryRepository {
    public NumberDictionaryRepositoryRepository(Path dectionaryPath) {
        super(dectionaryPath);
    }
}
